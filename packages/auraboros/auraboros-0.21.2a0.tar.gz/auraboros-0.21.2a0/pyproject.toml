[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "auraboros"
version = "0.21.2-alpha"
dependencies = ["pygame", "moderngl"]
requires-python = ">=3.10.8"

# [tool.hatch.build]
[project.scripts]
pyinstaller40 = "auraboros:get_hook_dirs"
