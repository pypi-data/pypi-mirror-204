---
// wrapper around @astrodown/graph so that the css is scoped
import { GraphinData, Network } from "@astrodown/graph";
import "@astrodown/graph/dist/style.css";
interface Props {
  data: GraphinData;
}

const { data } = Astro.props;
---

<Network client:only="react" data={data} />
