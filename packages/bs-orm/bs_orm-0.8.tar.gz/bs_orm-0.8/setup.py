from setuptools import setup

setup(name='bs_orm',
      version='0.8',
      description='better simple orm (everyone can take part in the development)',
      packages=['bs_orm'],
      author_email='mr.z.75@mail.ru',
      zip_safe=False)