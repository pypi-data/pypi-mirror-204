[tool.poetry]
name = "wrts"
version = "0.5.0"
description = "A small lil library for using the WRTS API "
authors = ["BadPythonCoder <no@no.no>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.7"
requests = "^2.28.2"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
