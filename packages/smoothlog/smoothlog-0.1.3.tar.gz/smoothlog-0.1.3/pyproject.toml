[tool.poetry]
name="smoothlog"
version="0.1.3"
authors =  ["ColGroLoOl"]
description="A nice and easy-to-use logger for python"
readme="README.md"
packages = [{include = "smoothlog"}]

[tool.poetry.dependencies]
python = ">=3.8"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"