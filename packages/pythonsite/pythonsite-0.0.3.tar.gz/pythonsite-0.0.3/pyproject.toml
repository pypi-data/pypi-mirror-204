[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "pythonsite"
version = "0.0.3"
authors = [
  { name="karpi", email="karpiworld@gmail.com" },
]
description = "A simple python web framework to create webpages"
readme = "README.md"
requires-python = ">=3.7"
classifiers = [
    "Programming Language :: Python :: 3",
    "License :: OSI Approved :: MIT License",
    "Operating System :: OS Independent",
]
