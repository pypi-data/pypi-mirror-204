[tool.poetry]
name = "db-components"
version = "0.1.0"
description = ""
authors = ["Dan Kelleher <kelleherjdan@gmail.com>"]
readme = "README.md"
packages = [{include = "db_components"}]

[tool.poetry.dependencies]
python = "^3.10"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
