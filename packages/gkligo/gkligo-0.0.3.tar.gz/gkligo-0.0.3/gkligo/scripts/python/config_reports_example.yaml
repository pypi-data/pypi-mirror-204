version: 1

# NOTE: We only need read-only access to the databases to generate the reports.

databases:
    atlas:
        username: atlasusername
        password: atlaspassword
        hostname: atlashostname
        database: atlasdbname

databases:
    ps:
        username: psusername
        password: pspassword
        hostname: pshostname
        database: psdbname

databases:
    pso4:
        username: pso4username
        password: pso4password
        hostname: pso4hostname
        database: pso4dbname

