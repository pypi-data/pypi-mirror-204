# [build-system]
# requires = ["hatchling"]
# build-backend = "hatchling.build"

# [project]
# name = "E.py"
# version = "0.0.1"
# authors = [
#   { name="E.py_CoderElijah", email="" },
# ]
# description = "My custom Python library."
# readme = "README.md"
# requires-python = ">=3.7"
# classifiers = [
#     "Programming Language :: Python :: 3",
#     "License :: OSI Approved :: MIT License",
#     "Operating System :: OS Independent",
# ]

[project.urls]
Homepage = "https://epy.coderelijah.repl.co"
BugTracker = "https://replit.com/@coderelijah/epy?v=1"

[tool.poetry]
name = "CoderElijah"
# name = "CoderElijahBeta"
version = "0.3.7"
description = "This is the custom Python library of @CoderElijah on https://replit.com/."
authors = ["CoderElijah <CoderElijah@outlook.com>"]
packages = [{include = "CoderElijah"}]

[tool.poetry.dependencies]
python = ">=3.8"
numpy = "^1.22.2"
replit = "^3.2.4"
Flask = "^2.2.0"
urllib3 = "^1.26.12"

[tool.poetry.dev-dependencies]
debugpy = "^1.6.2"
replit-python-lsp-server = {extras = ["yapf", "rope", "pyflakes"], version = "^1.5.0"}
toml = "^0.10.2"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"