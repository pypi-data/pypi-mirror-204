from setuptools import setup, find_packages

setup(
    name='timestamp-utils',
    version='0.1',
    description='Package for generating Unix timestamps and related timestamp formats',
    author='Your Name',
    author_email='your-email@example.com',
    packages=find_packages(),
    install_requires=[],
)
